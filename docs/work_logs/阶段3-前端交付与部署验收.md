# 阶段3：前端交付与部署验收工作日志

## 基本信息

- 日期：2026-09-24
- 实施范围：TPM 页面、安全与网关控制、可观测性、任务恢复、Docker 双实例和本地最终验收
- 联调边界：阶段3期间未调用真实飞书 API，未发送真实消息

## 前置验收

- 阶段1、阶段2代码与 Mock 测试通过，工作日志完整；
- 预览、发送、查询和失败重试接口保持统一 `success/code/message/data` 外壳；
- 真实卡片联调、公司网关、日志平台和告警平台按用户安排置于阶段3之后，不阻塞本地与 Mock 交付。

## 页面改动

- 完成输入、加载、只读预览、汇总、逐人业务领域、异常行、二次确认、发送进度、结果和仅重试失败项状态；
- 使用 `sessionStorage` 仅保存 `previewId`，刷新后从服务端恢复，不缓存完整接收人数据；
- 零待催项禁用发送，快照失效引导重新预览；
- 页面业务数据全部通过 `textContent` 构建，避免 HTML 注入；
- 完成桌面视觉检查，页面布局正常且浏览器控制台无错误。

## 安全与可观测性

- 新增 CSRF 双提交校验、Host 白名单、Redis 共享限流、CSP、禁止嵌入、MIME 嗅探与 Referrer Policy；
- 新增请求链路 ID，结构化日志包含 `requestId` 和 `previewId` 且沿用脱敏规则；
- 新增统一外壳的 `/internal/metrics`，提供 HTTP、预览、发送、限流、令牌、Redis 和停滞任务指标；
- 新增厂商无关告警规则样例；
- `.env` 未进入源码、测试输出、镜像或 Docker 构建上下文。

## 可靠性补充

- Redis 活跃任务集合记录 `SENDING/RETRYING` 任务；
- 多实例周期扫描并通过唯一锁恢复中断任务；
- 恢复时只发送未成功人员；
- 飞书消息请求使用按 `previewId + open_id` 派生的稳定 UUID 幂等键，自动重试和恢复共用同一键。

## 容器与部署

- Docker 镜像使用非 root 用户，内置存活检查并禁用重复访问日志；
- Compose 启动两个应用实例、共享 Redis 和本地 Nginx 验收网关；
- 网关仅绑定 `127.0.0.1:8000`，应用与 Redis 不直接映射宿主机端口；
- 应用容器启用只读根文件系统、临时 `/tmp`、能力删除和 `no-new-privileges`；
- 已提供环境变量、发布、回滚、排障和上线清单文档。

## 执行命令与结果

```powershell
.venv\Scripts\python.exe -m ruff check app tests
.venv\Scripts\python.exe -m pytest -q
node --check app/static/app.js
docker compose build
docker compose up -d
docker compose ps
Invoke-RestMethod http://127.0.0.1:8000/health/live
Invoke-RestMethod http://127.0.0.1:8000/health/ready
```

- Ruff：通过；
- pytest：61项通过；
- JavaScript语法：通过；
- 镜像构建：通过；
- 两个应用实例、Redis、网关：全部健康；
- 存活与就绪：均返回统一成功外壳；
- 双容器共享 Redis 竞争：实例一抢占成功，实例二返回 `EXISTS`，未重复创建任务；测试键已清理；
- 镜像安全：运行 UID=100；不存在 `/app/.env` 和 `/app/tests`；镜像历史未发现 Secret 特征。

## 已知提示与限制

- 自动化测试存在1条 FastAPI TestClient 依赖链的 Starlette/httpx 弃用提示；
- 未验证真实飞书卡片渲染、按钮跳转、真实限流及业务错误码；
- 未实际连接公司网关、日志或告警平台，提供的是接入契约和规则样例；
- 本地 Compose 正在运行，可用于后续真实联调，真实请求前仍需提供脱敏表格链接、阶段和测试接收人授权。

## 阶段结论

阶段3的代码、本地页面、Mock、安全、可观测性、容器和双实例验收已完成。项目已具备真实 API 联调条件，但一期生产验收仍以真实卡片和公司基础设施接入完成为准。
